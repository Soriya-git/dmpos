// This file is auto-generated by Laravel Wayfinder.
// Do not edit this file directly, any changes will be overwritten.

import {
    applyUrlDefaults,
    formSafeOptions,
    queryParams,
    type RouteDefinition,
    type RouteFormDefinition,
    type RouteQueryOptions,
} from './../../../../index';

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::reprint
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:433
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/reprint"
 */
export const reprint = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteDefinition<'post'> => ({
    url: reprint.url(args, options),
    method: 'post',
});

reprint.definition = {
    methods: ['post'],
    url: 'http://localhost/orders/{diningSession}/print-jobs/{printJob}/reprint',
} satisfies RouteDefinition<['post']>;

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::reprint
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:433
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/reprint"
 */
reprint.url = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
) => {
    if (Array.isArray(args)) {
        args = {
            diningSession: args[0],
            printJob: args[1],
        };
    }

    args = applyUrlDefaults(args);

    const parsedArgs = {
        diningSession:
            typeof args.diningSession === 'object'
                ? args.diningSession.id
                : args.diningSession,
        printJob:
            typeof args.printJob === 'object'
                ? args.printJob.id
                : args.printJob,
    };

    return (
        reprint.definition.url
            .replace('{diningSession}', parsedArgs.diningSession.toString())
            .replace('{printJob}', parsedArgs.printJob.toString())
            .replace(/\/+$/, '') + queryParams(options)
    );
};

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::reprint
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:433
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/reprint"
 */
reprint.post = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteDefinition<'post'> => ({
    url: reprint.url(args, options),
    method: 'post',
});

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::reprint
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:433
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/reprint"
 */
const reprintForm = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteFormDefinition<'post'> => ({
    action: reprint.url(args, options),
    method: 'post',
});

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::reprint
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:433
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/reprint"
 */
reprintForm.post = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteFormDefinition<'post'> => ({
    action: reprint.url(args, options),
    method: 'post',
});

reprint.form = reprintForm;
/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
export const preview = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteDefinition<'get'> => ({
    url: preview.url(args, options),
    method: 'get',
});

preview.definition = {
    methods: ['get', 'head'],
    url: 'http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview',
} satisfies RouteDefinition<['get', 'head']>;

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
preview.url = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
) => {
    if (Array.isArray(args)) {
        args = {
            diningSession: args[0],
            printJob: args[1],
        };
    }

    args = applyUrlDefaults(args);

    const parsedArgs = {
        diningSession:
            typeof args.diningSession === 'object'
                ? args.diningSession.id
                : args.diningSession,
        printJob:
            typeof args.printJob === 'object'
                ? args.printJob.id
                : args.printJob,
    };

    return (
        preview.definition.url
            .replace('{diningSession}', parsedArgs.diningSession.toString())
            .replace('{printJob}', parsedArgs.printJob.toString())
            .replace(/\/+$/, '') + queryParams(options)
    );
};

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
preview.get = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteDefinition<'get'> => ({
    url: preview.url(args, options),
    method: 'get',
});

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
preview.head = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteDefinition<'head'> => ({
    url: preview.url(args, options),
    method: 'head',
});

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
const previewForm = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteFormDefinition<'get'> => ({
    action: preview.url(args, options),
    method: 'get',
});

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
previewForm.get = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteFormDefinition<'get'> => ({
    action: preview.url(args, options),
    method: 'get',
});

/**
 * @see \App\Http\Controllers\Seats\SeatOrderController::previewPrintJob
 * @see /home/runner/work/dmpos/dmpos/app/Http/Controllers/Seats/SeatOrderController.php:466
 * @route "http://localhost/orders/{diningSession}/print-jobs/{printJob}/preview"
 */
previewForm.head = (
    args:
        | {
              diningSession: number | { id: number };
              printJob: number | { id: number };
          }
        | [
              diningSession: number | { id: number },
              printJob: number | { id: number },
          ],
    options?: RouteQueryOptions,
): RouteFormDefinition<'get'> => ({
    action: preview.url(args, formSafeOptions('head', options)),
    method: 'get',
});

preview.form = previewForm;

const printJobs = {
    reprint,
    preview,
};

export default printJobs;
